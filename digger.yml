projects:
- name: production
  dir: prod
  workflow: with-infracost

workflows:
  with-infracost:
    plan:
      steps:
        - init
        - plan
        - run: echo $PROJECT_NAME $PROJECT_DIR $PR_BRANCH $DEFAULT_BRANCH > $DIGGER_OUT
